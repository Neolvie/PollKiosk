# Poll Kiosk - Интерактивное голосование для стенда

Система для проведения голосований на выставочных стендах с красивым UI в фиолетово-сиреневой палитре Directum Ario.

## Возможности

- 📊 **Публичный экран голосования** - крупный текст, адаптивный дизайн для больших экранов
- 🎨 **Красивый UI** - фиолетово-сиреневая палитра, градиенты, анимации
- ⚡ **Мгновенное голосование** - выбор варианта сразу отправляет голос
- 🔒 **Бэк-офис** - управление опросами с HTTP Basic Auth
- 📈 **Статистика** - просмотр результатов голосования в реальном времени
- 💾 **SQLite база данных** - все голоса сохраняются

## Структура проекта

```
poll-kiosk/
├── app.py                 # Flask backend
├── database.py            # SQLite операции
├── config.json            # Конфигурация (логин/пароль админа)
├── templates/
│   ├── index.html         # Публичный экран голосования
│   └── admin.html         # Бэк-офис (управление и статистика)
└── polls.db              # SQLite база данных (создается автоматически)
```

## Установка и запуск

### 1. Установите зависимости

```bash
pip install flask
```

### 2. Запустите приложение

```bash
python app.py
```

Приложение запустится на `http://localhost:5000`

### 3. Доступ

- **Публичный экран**: `http://localhost:5000/`
- **Бэк-офис**: `http://localhost:5000/admin`
  - Логин: `admin`
  - Пароль: `changeme` (измените в `config.json`)

## Использование

### Создание первого опроса

1. Откройте бэк-офис: `http://localhost:5000/admin`
2. Введите логин и пароль
3. Нажмите "Создать новый опрос"
4. Введите вопрос и варианты ответов
5. Нажмите "Создать"
6. Выберите созданный опрос и нажмите "Активировать"

### Проведение голосования

1. Откройте публичный экран на стенде
2. Посетители видят вопрос и варианты
3. При выборе варианта голос сразу отправляется
4. Показывается сообщение "Спасибо за ваше мнение"
5. Через 3 секунды экран готов к следующему голосованию

### Просмотр статистики

1. В бэк-офисе нажмите "Статистика" у нужного опроса
2. Увидите:
   - Общее количество голосов
   - Распределение по вариантам ответов
   - Последние 20 голосов с временем

## API Endpoints

### Публичные

- `GET /` - публичный экран голосования
- `GET /api/current-poll` - получить текущий активный опрос
- `POST /api/vote` - отправить голос

### Административные (требуют авторизации)

- `GET /admin` - бэк-офис
- `GET /api/admin/polls` - список всех опросов
- `POST /api/admin/polls` - создать опрос
- `DELETE /api/admin/polls/<id>` - удалить опрос
- `POST /api/admin/set-current-poll` - установить текущий опрос
- `GET /api/admin/stats/<id>` - статистика по опросу

## Конфигурация

Отредактируйте `config.json`:

```json
{
  "admin_username": "admin",
  "admin_password": "ваш_пароль",
  "current_poll_id": 1
}
```

## Масштабирование для больших экранов

Интерфейс автоматически адаптируется:
- **До 1920px**: стандартный размер
- **1920px и выше**: увеличенные шрифты и элементы
- **Широкоформатный**: 16:9, оптимизирован для стендов

## Безопасность

- HTTP Basic Auth для бэк-офиса
- Валидация всех входящих данных
- SQL injection защита через параметризованные запросы
- Логирование IP адресов голосующих

## Развитие

### Планируемые улучшения

- Экспорт статистики в Excel/CSV
- Графики и диаграммы в бэк-офисе
- Таймер для автоматической смены опросов
- Поддержка изображений в вопросах
- Мультиязычность

## Технологии

- **Backend**: Flask (Python)
- **Database**: SQLite
- **Frontend**: Vanilla HTML/CSS/JS
- **Auth**: HTTP Basic Authentication
- **Design**: Custom CSS с фиолетово-сиреневой палитрой

## Лицензия

MIT

## Автор

Создано для Directum Ario
